<!DOCTYPE html>
<html lang="en">
<head>
         <title>DSM| TMD </title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" sizes="56x56"
    href="../images/fav-icon/Black White Minimalist Professional Initial Logo.png">
        <meta name="robots" content="noindex, nofollow">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4361ee;
            --dark-bg: #171819;
            --card-bg: #2d2d2d;
            --card-hover: #3d3d3d;
            --text-light: #f8f9fa;
        }
        
        body {
            background-color: var(--dark-bg);
            color: var(--text-light);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding-top: 70px;
        }
        
        .dashboard-card {
            background: var(--card-bg);
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            transition: all 0.3s;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            max-width: 800px;
            margin: 0 auto;
        }
        
        .dashboard-card:hover {
            background: var(--card-hover);
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        }
        
        .arrow-button {
            position: fixed;
            top: 20px;
            left: 20px;
            width: 50px;
            height: 50px;
            background: var(--primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
            color: white;
            font-size: 1.2rem;
            z-index: 100;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .arrow-button:hover {
            transform: scale(1.1);
            background: white;
            color: var(--primary);
        }
        
        .dashboard-header {
            border-bottom: 1px solid #444;
            padding-bottom: 15px;
            margin-bottom: 25px;
            text-align: center;
        }
        
        .form-control, textarea {
            background-color: #3a3a3a;
            border: 1px solid #4a4a4a;
            color: white;
            width: 100%;
            padding: 10px;
            border-radius: 4px;
        }
        
        textarea {
            min-height: 120px;
        }
        
        .form-control:focus, textarea:focus {
            background-color: #4a4a4a;
            color: white;
            border-color: var(--primary);
            box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.25);
        }
        
        .btn-primary {
            background-color: var(--primary);
            border-color: var(--primary);
            width: 100%;
            padding: 12px;
            font-size: 16px;
        }
        
        .btn-primary:hover {
            background-color: #3a56d4;
            border-color: #3a56d4;
        }
    </style>
</head>
<body>
    <a href="/dashboard-Admin03">
        <button class="arrow-button" aria-label="Go back">
            <i class="fas fa-arrow-left"></i>
        </button>
    </a>

    <div class="container py-4">
        <div class="dashboard-header">
            <h1><i class="fas fa-newspaper me-2"></i>Banner Dashboard</h1>
        </div>
        
        <div class="dashboard-card">
            <form action="/banner?_method=PUT" method="POST">
                <div class="mb-3">
                    <label for="title" class="form-label">Title</label>
                    <input type="text" class="form-control" id="title" name="title" value="<%= banner.title %>" required>
                </div>
                
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea class="form-control" id="description" name="description" rows="5" required><%= banner.description %></textarea>
                </div>
                
                <div class="mb-3">
                    <label for="link" class="form-label">Button Link</label>
                    <input type="text" class="form-control" id="link" name="link" value="<%= banner.link %>" required>
                </div>
                
                <div class="mb-3">
                    <label for="buttonText" class="form-label">Button Text</label>
                    <input type="text" class="form-control" id="buttonText" name="buttonText" value="<%= banner.buttonText %>" required>
                </div>
                
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save me-1"></i> Update Banner
                </button>
            </form>
            
            <div class="text-center mt-3">
                <a href="/" class="btn btn-outline-light">
                    <i class="fas fa-eye me-1"></i> Preview on Homepage
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
