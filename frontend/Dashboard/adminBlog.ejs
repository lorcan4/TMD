<!DOCTYPE html>
<html lang="en">
<head>
         <title>DSM| TMD </title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #4f46e5;
            --primary-dark: #4338ca;
            --secondary: #6b7280;
            --secondary-dark: #4b5563;
            --danger: #ef4444;
            --danger-dark: #dc2626;
            --success: #10b981;
            --warning: #f59e0b;
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, var(--primary) 0%, #7c3aed 100%);
        }
        
        .shadow-soft {
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        
        .shadow-hover {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        
        .transition-all {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .hover-scale:hover {
            transform: translateY(-2px);
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 500;
            border-radius: 0.5rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .btn-primary {
            background-color: var(--primary);
            color: white;
        }
        
        .btn-primary:hover {
            background-color: var(--primary-dark);
            box-shadow: 0 10px 15px -3px rgba(79, 70, 229, 0.3), 0 4px 6px -2px rgba(79, 70, 229, 0.2);
        }
        
        .btn-secondary {
            background-color: var(--secondary);
            color: white;
        }
        
        .btn-secondary:hover {
            background-color: var(--secondary-dark);
        }
        
        .btn-danger {
            background-color: var(--danger);
            color: white;
        }
        
        .btn-danger:hover {
            background-color: var(--danger-dark);
        }
        
        .input-field {
            border: 1px solid #e2e8f0;
            border-radius: 0.5rem;
            padding: 0.75rem 1rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .input-field:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.2);
            outline: none;
        }
        
        .file-upload {
            border: 2px dashed #e2e8f0;
            border-radius: 0.5rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .file-upload:hover {
            border-color: var(--primary);
            background-color: #f8fafc;
        }
        
        .badge {
            display: inline-flex;
            align-items: center;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: capitalize;
        }
        
        .badge-technology {
            background-color: #dbeafe;
            color: #1d4ed8;
        }
        
        .badge-business {
            background-color: #dcfce7;
            color: #166534;
        }
        
        .badge-health {
            background-color: #ede9fe;
            color: #5b21b6;
        }
        
        .badge-entertainment {
            background-color: #fef3c7;
            color: #92400e;
        }
        
        .badge-politics {
            background-color: #fee2e2;
            color: #991b1b;
        }
        
        .table-hover tr:hover {
            background-color: #f8fafc;
        }
        
        .line-clamp {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        @media (max-width: 768px) {
            .responsive-table {
                display: block;
                overflow-x: auto;
                white-space: nowrap;
            }
        }
         /* Fixed Back Arrow */
    .arrow-button {
        position: fixed;
        top: 15px;
        left: 15px;
        width: 45px;
        height: 45px;
        background: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        color: var(--primary);
        font-size: 1.1rem;
        z-index: 100;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      
      .arrow-button:hover {
        transform: scale(1.05);
        background: var(--primary);
        color: white;
      }
    </style>
</head>
<a href="/admin-access-panel-03">
    <button class="arrow-button" aria-label="Go back">
      <i class="fas fa-arrow-left"></i>
    </button>
  </a>
<body class="font-sans antialiased">
    <div class="min-h-screen">
        <!-- Header -->
        <!-- Mobile Menu -->
        <div class="md:hidden hidden bg-white shadow-md" id="mobile-menu">
            <div class="px-4 py-3 space-y-2">
                <a href="/admin" class="block px-3 py-2 text-gray-700 hover:bg-gray-100 rounded">
                    <i class="fas fa-tachometer-alt mr-2"></i> Dashboard
                </a>
                <a href="/" class="block px-3 py-2 text-gray-700 hover:bg-gray-100 rounded">
                    <i class="fas fa-globe mr-2"></i> View Site
                </a>
                <div class="border-t border-gray-200 pt-2">
                    <a href="#" class="block px-3 py-2 text-gray-700 hover:bg-gray-100 rounded">
                        <i class="fas fa-user-circle mr-2"></i> Profile
                    </a>
                    <a href="/logout" class="block px-3 py-2 text-gray-700 hover:bg-gray-100 rounded">
                        <i class="fas fa-sign-out-alt mr-2"></i> Logout
                    </a>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <main class="container mx-auto px-4 py-8">
            <!-- Form Section -->
            <section class="mb-12">
                <div class="bg-white rounded-xl shadow-soft overflow-hidden transition-all hover:shadow-hover">
                    <div class="gradient-bg px-6 py-4 text-white">
                        <div class="flex items-center">
                            <i class="fas fa-edit text-xl mr-3"></i>
                            <h2 class="text-xl font-semibold">
                                <%= editing ? 'Edit Post' : 'Create New Post' %>
                            </h2>
                        </div>
                    </div>
                    
                    <div class="p-6">
                        <form action="<%= editing ? `/posts/${post._id}?_method=PUT` : '/posts' %>" 
                              method="POST" 
                              enctype="multipart/form-data"
                              class="space-y-6">
                            
                            <% if (editing) { %>
                                <input type="hidden" name="existingImage" value="<%= post.imageUrl %>">
                            <% } %>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                <div>
                                    <label class="block text-gray-700 font-medium mb-2">Title *</label>
                                    <input type="text" name="title" value="<%= post ? post.title : '' %>" 
                                           class="w-full input-field" required>
                                </div>
                                
                                <div>
                                    <label class="block text-gray-700 font-medium mb-2">Category *</label>
                                    <select name="category" class="w-full input-field">
                                        <option value="Technology" <%= post && post.category === 'Technology' ? 'selected' : '' %>>Technology</option>
                                        <option value="Business" <%= post && post.category === 'Business' ? 'selected' : '' %>>Business</option>
                                        <option value="Health" <%= post && post.category === 'Health' ? 'selected' : '' %>>Health</option>
                                        <option value="Entertainment" <%= post && post.category === 'Entertainment' ? 'selected' : '' %>>Entertainment</option>
                                        <option value="Politics" <%= post && post.category === 'Politics' ? 'selected' : '' %>>Politics</option>
                                    </select>
                                </div>
                            </div>
                            
                            <div>
                                <label class="block text-gray-700 font-medium mb-2">Image <%= editing ? '' : '*' %></label>
                                <% if (editing && post.imageUrl) { %>
                                    <div class="mb-4 flex items-center space-x-4">
                                        <img src="<%= post.imageUrl %>" alt="Current image" 
                                             class="rounded-lg w-32 h-32 object-cover shadow-sm border border-gray-200">
                                        <div>
                                            <p class="text-sm font-medium text-gray-700">Current Image</p>
                                            <p class="text-xs text-gray-500">Upload new image to replace</p>
                                        </div>
                                    </div>
                                <% } %>
                                <div class="file-upload flex items-center justify-center w-full h-32">
                                    <label class="flex flex-col items-center justify-center w-full h-full cursor-pointer">
                                        <div class="flex flex-col items-center justify-center">
                                            <i class="fas fa-cloud-upload-alt text-3xl text-gray-400 mb-2"></i>
                                            <p class="text-sm text-gray-500">
                                                <span class="font-semibold">Click to upload</span> or drag and drop
                                            </p>
                                            <p class="text-xs text-gray-400 mt-1">PNG, JPG, GIF up to 5MB</p>
                                        </div>
                                        <input type="file" name="image" class="hidden" <%= editing ? '' : 'required' %>>
                                    </label>
                                </div>
                            </div>
                            
                            <div>
                                <label class="block text-gray-700 font-medium mb-2">Short Description *</label>
                                <textarea name="description" rows="3" 
                                          class="w-full input-field" 
                                          placeholder="A brief summary that will appear in post listings"
                                          required><%= post ? post.description : '' %></textarea>
                            </div>
                            
                            <div>
                                <label class="block text-gray-700 font-medium mb-2">Full Content *</label>
                                <textarea name="content" rows="8" 
                                          class="w-full input-field" 
                                          placeholder="Write your full post content here..."
                                          required><%= post ? post.content : '' %></textarea>
                            </div>
                            
                            <div>
                                <label class="block text-gray-700 font-medium mb-2">Author</label>
                                <input type="text" name="author" value="<%= post ? post.author : 'Admin' %>" 
                                       class="w-full input-field" placeholder="Post author name">
                            </div>
                            
                            <div class="flex flex-col sm:flex-row space-y-4 sm:space-y-0 sm:space-x-4 pt-2">
                                <button type="submit" 
                                        class="btn btn-primary px-6 py-3 hover-scale">
                                    <i class="fas fa-save mr-2"></i>
                                    <%= editing ? 'Update Post' : 'Publish Post' %>
                                </button>
                                <% if (editing) { %>
                                    <a href="/admin" 
                                       class="btn btn-secondary px-6 py-3 hover-scale text-center">
                                        <i class="fas fa-times mr-2"></i>
                                        Cancel
                                    </a>
                                <% } else { %>
                                    <button type="reset" 
                                            class="btn btn-secondary px-6 py-3 hover-scale">
                                        <i class="fas fa-undo mr-2"></i>
                                        Reset Form
                                    </button>
                                <% } %>
                            </div>
                        </form>
                    </div>
                </div>
            </section>
            
            <!-- Posts Table Section -->
            <section>
                <div class="bg-white rounded-xl shadow-soft overflow-hidden">
                    <div class="gradient-bg px-6 py-4 text-white">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center">
                                <i class="fas fa-list text-xl mr-3"></i>
                                <h2 class="text-xl font-semibold">Current Posts</h2>
                            </div>
                            <div class="text-sm">
                                Total: <span class="font-bold"><%= posts.length %></span> posts
                            </div>
                        </div>
                    </div>
                    
                    <div class="p-6">
                        <% if (posts.length === 0) { %>
                            <div class="text-center py-12">
                                <i class="fas fa-newspaper text-5xl text-gray-300 mb-4"></i>
                                <h3 class="text-lg font-medium text-gray-500">No posts found</h3>
                                <p class="text-gray-400 mt-1">Get started by creating your first post</p>
                                <a href="/blog-manager-portal-57" class="btn btn-primary mt-4 px-6 py-2 inline-block">
                                    <i class="fas fa-plus mr-2"></i> Create Post
                                </a>
                            </div>
                        <% } else { %>
                            <div class="mb-4 flex flex-col sm:flex-row justify-between items-start sm:items-center">
                                <div class="relative w-full sm:w-64 mb-4 sm:mb-0">
                                    <input type="text" placeholder="Search posts..." 
                                           class="w-full pl-10 pr-4 py-2 border rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
                                    <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
                                </div>
                                <div class="flex space-x-2">
                                    <select class="border rounded-lg px-3 py-2 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
                                        <option>All Categories</option>
                                        <option>Technology</option>
                                        <option>Business</option>
                                        <option>Health</option>
                                        <option>Entertainment</option>
                                        <option>Politics</option>
                                    </select>
                                    <button class="btn btn-primary px-4 py-2">
                                        <i class="fas fa-filter mr-2"></i> Filter
                                    </button>
                                </div>
                            </div>
                            
                            <div class="responsive-table">
                                <table class="min-w-full divide-y divide-gray-200">
                                    <thead class="bg-gray-50">
                                        <tr>
                                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Image</th>
                                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Title & Description</th>
                                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Category</th>
                                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody class="bg-white divide-y divide-gray-200 table-hover">
                                        <% posts.forEach(post => { %>
                                        <tr class="transition-all hover:bg-gray-50">
                                            <td class="px-6 py-4 whitespace-nowrap">
                                                <img src="<%= post.imageUrl %>" alt="<%= post.title %>" 
                                                     class="h-12 w-12 object-cover rounded-md shadow-sm">
                                            </td>
                                            <td class="px-6 py-4">
                                                <div class="text-sm font-medium text-gray-900"><%= post.title %></div>
                                                <div class="text-sm text-gray-500 line-clamp"><%= post.description %></div>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap">
                                                <span class="badge badge-<%= post.category.toLowerCase() %>">
                                                    <i class="fas 
                                                        <%= post.category === 'Technology' ? 'fa-microchip' : 
                                                           post.category === 'Business' ? 'fa-briefcase' : 
                                                           post.category === 'Health' ? 'fa-heartbeat' : 
                                                           post.category === 'Entertainment' ? 'fa-film' : 
                                                           'fa-landmark' %> mr-1"></i>
                                                    <%= post.category %>
                                                </span>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap">
                                                <div class="text-sm text-gray-900 font-medium"><%= post.date.toLocaleDateString() %></div>
                                                <div class="text-xs text-gray-500"><%= post.date.toLocaleTimeString() %></div>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap">
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                    <i class="fas fa-check-circle mr-1"></i>
                                                    Published
                                                </span>
                                            </td>
                                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                                <div class="flex space-x-3">
                                                    <a href="/blog-manager-portal-57?edit=<%= post._id %>" 
                                                       class="text-indigo-600 hover:text-indigo-900 transition-all"
                                                       title="Edit post">
                                                        <i class="fas fa-edit"></i>
                                                    </a>
                                                    <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" class="inline">
                                                        <button type="submit" 
                                                                class="text-red-600 hover:text-red-900 transition-all"
                                                                title="Delete post"
                                                                onclick="return confirm('Are you sure you want to delete this post?')">
                                                            <i class="fas fa-trash-alt"></i>
                                                        </button>
                                                    </form>
                                                    <!-- <a href="/post/<%= post._id %>" 
                                                       class="text-green-600 hover:text-green-900 transition-all"
                                                       title="View post"
                                                       target="_blank">
                                                        <i class="fas fa-external-link-alt"></i>
                                                    </a> -->
                                                </div>
                                            </td>
                                        </tr>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                            
                            <!-- Pagination -->
                            <div class="flex items-center justify-between mt-6">
                                <div class="text-sm text-gray-500">
                                    Showing <span class="font-medium">1</span> to <span class="font-medium">10</span> of <span class="font-medium"><%= posts.length %></span> results
                                </div>
                                <div class="flex space-x-2">
                                    <button class="px-3 py-1 rounded-md border border-gray-300 text-gray-500 hover:bg-gray-50 disabled:opacity-50" disabled>
                                        Previous
                                    </button>
                                    <button class="px-3 py-1 rounded-md border border-indigo-500 bg-indigo-50 text-indigo-600">
                                        1
                                    </button>
                                    <button class="px-3 py-1 rounded-md border border-gray-300 text-gray-500 hover:bg-gray-50">
                                        2
                                    </button>
                                    <button class="px-3 py-1 rounded-md border border-gray-300 text-gray-500 hover:bg-gray-50">
                                        Next
                                    </button>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <script>
        // Enhanced functionality
        document.addEventListener('DOMContentLoaded', function() {
            // Mobile menu toggle
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const mobileMenu = document.getElementById('mobile-menu');
            
            mobileMenuButton.addEventListener('click', function() {
                mobileMenu.classList.toggle('hidden');
            });
            
            // Delete confirmation
            const deleteForms = document.querySelectorAll('form[action*="delete"]');
            deleteForms.forEach(form => {
                form.addEventListener('submit', function(e) {
                    e.preventDefault();
                    
                    if (confirm('Are you sure you want to delete this post? This action cannot be undone.')) {
                        this.submit();
                    }
                });
            });
            
            // File upload preview
            const fileInputs = document.querySelectorAll('input[type="file"]');
            fileInputs.forEach(input => {
                input.addEventListener('change', function() {
                    const file = this.files[0];
                    if (file) {
                        const reader = new FileReader();
                        const previewContainer = this.closest('.file-upload');
                        
                        reader.onload = function(e) {
                            // Remove existing preview if any
                            const existingPreview = previewContainer.querySelector('.image-preview');
                            if (existingPreview) {
                                existingPreview.remove();
                            }
                            
                            // Create preview element
                            const preview = document.createElement('div');
                            preview.className = 'image-preview mt-4 flex items-center space-x-4';
                            preview.innerHTML = `
                                <img src="${e.target.result}" alt="Preview" class="rounded-lg w-32 h-32 object-cover shadow-sm border border-gray-200">
                                <div>
                                    <p class="text-sm font-medium text-gray-700">${file.name}</p>
                                    <p class="text-xs text-gray-500">${(file.size / 1024 / 1024).toFixed(2)} MB</p>
                                </div>
                            `;
                            
                            // Hide upload prompt
                            const uploadPrompt = previewContainer.querySelector('label > div');
                            if (uploadPrompt) {
                                uploadPrompt.style.display = 'none';
                            }
                            
                            // Add preview to container
                            previewContainer.querySelector('label').appendChild(preview);
                        }
                        
                        reader.readAsDataURL(file);
                    }
                });
            });
            
            // Search functionality
            const searchInput = document.querySelector('input[placeholder="Search posts..."]');
            if (searchInput) {
                searchInput.addEventListener('input', function() {
                    const searchTerm = this.value.toLowerCase();
                    const rows = document.querySelectorAll('tbody tr');
                    
                    rows.forEach(row => {
                        const title = row.querySelector('td:nth-child(2) div:first-child').textContent.toLowerCase();
                        const description = row.querySelector('td:nth-child(2) div:last-child').textContent.toLowerCase();
                        
                        if (title.includes(searchTerm) || description.includes(searchTerm)) {
                            row.style.display = '';
                        } else {
                            row.style.display = 'none';
                        }
                    });
                });
            }
        });
    </script>
</body>
</html>
