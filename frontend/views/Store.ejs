<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Store | TMD GYM M'BARK</title>
<meta name="description" content="Shop for Muay Thai gear, fitness accessories, and exclusive TMD GYM merchandise. Explore our collection of quality products for your fitness journey.">
<meta name="keywords" content="TMD GYM Store, Muay Thai Gear, Fitness Accessories, Gym Merchandise, Fitness Equipment, TMD Store">

    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="icon" type="image/png" sizes="56x56"
    href="../images/fav-icon/Black White Minimalist Professional Initial Logo.png">
    <style>
        /* Sophisticated Color Palette */
        :root {
            --onyx: #393D3F;
            --jet: #2B2D42;
            --platinum: #E5E5E5;
            --flame: #E4572E;
            --viridian: #1B998B;
            --sapphire: #126D8E;
            --ivory: #F8F4E3;
            --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.12);
            --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.16);
            --shadow-lg: 0 10px 25px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            --border-radius: 8px;
            --border-radius-lg: 16px;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: var(--ivory);
            color: var(--onyx);
            font-family: 'Roboto', sans-serif;
            line-height: 1.6;
        }
        
        h1, h2, h3, h4, .logo, .category-nav a, .btn {
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
        }
        
        /* Elegant Container */
        .store-container {
            max-width: 1400px;
            margin: 0 auto;
            background-color: white;
            box-shadow: var(--shadow-lg);
            min-height: 100vh;
            position: relative;
        }
        
        /* Modern Header */
        .store-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 40px;
            background-color: white;
            box-shadow: var(--shadow-sm);
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .logo {
            display: flex;
            align-items: center;
            font-size: 28px;
            font-weight: 800;
            color: var(--jet);
            letter-spacing: 1px;
        }
        
        .logo i {
            margin-right: 12px;
            color: var(--flame);
            font-size: 32px;
        }
        
        .nav-links {
            display: flex;
            align-items: center;
            gap: 30px;
        }
        
        .dashboard-link {
            background-color: var(--flame);
            color: white;
            padding: 12px 24px;
            border-radius: var(--border-radius);
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            border: 2px solid transparent;
            box-shadow: var(--shadow-sm);
        }
        
        .dashboard-link:hover {
            background-color: white;
            color: var(--flame);
            border-color: var(--flame);
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }
        
        /* Minimalist Cart */
        .cart-link {
            position: relative;
            color: var(--jet);
            text-decoration: none;
            font-size: 22px;
            transition: var(--transition);
        }
        
        .cart-link:hover {
            color: var(--flame);
        }
        
        .cart-count {
            position: absolute;
            top: -8px;
            right: -12px;
            background-color: var(--flame);
            color: white;
            border-radius: 50%;
            width: 22px;
            height: 22px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: bold;
        }
        
        /* Hero - Elegant & Spacious */
        .hero {
            background: 
                linear-gradient(rgba(43, 45, 66, 0.85), 
                rgba(43, 45, 66, 0.85)),
                url('../images//home/photo-1571019613454-1cb2f99b2d8b.avif');
            background-size: cover;
            background-position: center;
            height: 500px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            margin-bottom: 60px;
            position: relative;
            overflow: hidden;
        }
        
        .hero::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 8px;
            background: linear-gradient(90deg, var(--flame), var(--viridian));
        }
        
        .hero-content {
            max-width: 800px;
            padding: 0 20px;
        }
        
        .hero-content h2 {
            font-size: 56px;
            margin-bottom: 20px;
            color: white;
            line-height: 1.2;
            font-weight: 700;
            text-transform: none;
        }
        
        .hero-content p {
            font-size: 20px;
            color: var(--platinum);
            margin: 0 auto 40px;
            font-weight: 300;
            letter-spacing: 0.5px;
        }
        
        /* Category Selector - Clean Tabs */
        .category-nav {
            display: flex;
            justify-content: center;
            margin-bottom: 60px;
            flex-wrap: wrap;
            gap: 15px;
            padding: 0 40px;
        }
        
        .category-nav a {
            color: var(--onyx);
            text-decoration: none;
            padding: 12px 30px;
            font-weight: 600;
            border-radius: var(--border-radius);
            transition: var(--transition);
            background-color: transparent;
            border: 2px solid var(--platinum);
            text-transform: none;
            letter-spacing: 0.5px;
        }
        
        .category-nav a:hover,
        .category-nav a.active {
            background-color: var(--flame);
            color: white;
            border-color: var(--flame);
            transform: translateY(-3px);
            box-shadow: var(--shadow-sm);
        }
        
        /* Product Grid - Card Design */
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 40px;
            padding: 0 40px;
            margin-bottom: 80px;
        }
        
        .product-card {
            background-color: white;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--shadow-sm);
            transition: var(--transition);
            position: relative;
            border: 1px solid rgba(0,0,0,0.05);
        }
        
        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-lg);
        }
        
        .product-image {
            height: 240px;
            overflow: hidden;
            position: relative;
        }
        
        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }
        
        .product-card:hover .product-image img {
            transform: scale(1.05);
        }
        
        .product-info {
            padding: 24px;
        }
        
        .product-info h3 {
            margin-bottom: 12px;
            color: var(--jet);
            font-size: 20px;
            font-weight: 600;
        }
        
        .product-info p {
            color: #666;
            margin-bottom: 20px;
            font-size: 15px;
            line-height: 1.5;
        }
        
        .product-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
        }
        
        .price {
            font-weight: 700;
            font-size: 22px;
            color: var(--flame);
        }
        
        .in-stock {
            color: var(--viridian);
            font-weight: 600;
            font-size: 14px;
        }
        
        .out-of-stock {
            color: var(--flame);
            font-weight: 600;
            font-size: 14px;
        }
        
        .btn-add-to-cart {
            background-color: var(--jet);
            color: white;
            border: none;
            padding: 14px;
            width: 100%;
            border-radius: var(--border-radius);
            cursor: pointer;
            font-weight: 600;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            letter-spacing: 0.5px;
        }
        
        .btn-add-to-cart:hover {
            background-color: var(--flame);
            transform: translateY(-2px);
            box-shadow: var(--shadow-sm);
        }
        
        /* Modern Footer */
        footer {
            text-align: center;
            padding: 60px 40px;
            background-color: var(--jet);
            color: var(--platinum);
            margin-top: 80px;
        }
        
        footer p {
            font-size: 14px;
            letter-spacing: 0.5px;
            opacity: 0.8;
        }
        
        /* Cart Section - Clean Design */
        .cart-section {
            max-width: 1000px;
            margin: 60px auto;
            padding: 40px;
            display: none;
            background-color: white;
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow-md);
            border-top: 8px solid var(--flame);
        }
        
        .cart-section h2 {
            color: var(--jet);
            font-size: 32px;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--platinum);
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .cart-items {
            margin-bottom: 40px;
        }
        
        .cart-item {
            display: flex;
            align-items: center;
            padding: 25px;
            background: white;
            border-radius: var(--border-radius);
            margin-bottom: 20px;
            box-shadow: var(--shadow-sm);
            border-left: 4px solid var(--flame);
            transition: var(--transition);
        }
        
        .cart-item:hover {
            transform: translateX(5px);
            box-shadow: var(--shadow-md);
        }
        
        .item-image {
            width: 100px;
            height: 100px;
            margin-right: 25px;
            border-radius: var(--border-radius);
            overflow: hidden;
            flex-shrink: 0;
        }
        
        .item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .item-details {
            flex: 1;
        }
        
        .item-details h3 {
            margin-bottom: 8px;
            color: var(--jet);
            font-weight: 600;
        }
        
        .item-details p {
            color: var(--flame);
            font-weight: 600;
            font-size: 18px;
        }
        
        .item-quantity {
            margin-top: 8px;
            font-size: 15px;
            color: #666;
        }
        
        .item-actions {
            margin-left: 25px;
        }
        
        .btn-remove {
            background: none;
            border: none;
            color: var(--flame);
            font-size: 22px;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .btn-remove:hover {
            transform: scale(1.2);
        }
        
        .cart-summary {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 25px;
            background: var(--platinum);
            border-radius: var(--border-radius);
        }
        
        .total {
            font-size: 24px;
            font-weight: 700;
            color: var(--jet);
        }
        
        .total span:last-child {
            color: var(--flame);
            margin-left: 10px;
        }
        
        .btn-checkout {
            background-color: var(--flame);
            color: white;
            padding: 16px 36px;
            border-radius: var(--border-radius);
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            border: none;
            cursor: pointer;
            letter-spacing: 0.5px;
            box-shadow: var(--shadow-sm);
        }
        
        .btn-checkout:hover {
            background-color: #d04a2b;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }
        
        .empty-cart {
            text-align: center;
            padding: 60px 40px;
            background: var(--platinum);
            border-radius: var(--border-radius);
        }
        
        .empty-cart p {
            font-size: 18px;
            margin-bottom: 30px;
            color: var(--jet);
        }
        
        .btn-continue {
            background-color: var(--jet);
            color: white;
            padding: 14px 30px;
            border-radius: var(--border-radius);
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            display: inline-block;
            box-shadow: var(--shadow-sm);
        }
        
        .btn-continue:hover {
            background-color: var(--flame);
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }
        
        /* Alerts - Modern Notifications */
        .alert {
            padding: 18px 25px;
            margin: 20px 40px;
            border-radius: var(--border-radius);
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 15px;
            border-left: 4px solid transparent;
            box-shadow: var(--shadow-sm);
        }
        
        .alert i {
            font-size: 22px;
        }
        
        .alert-error {
            background-color: #fdecea;
            color: #721c24;
            border-left-color: var(--flame);
        }
        
        .alert-success {
            background-color: #e8f5e9;
            color: #155724;
            border-left-color: var(--viridian);
        }
        
        /* Responsive Design */
        @media (max-width: 1024px) {
            .hero {
                height: 400px;
            }
            
            .hero-content h2 {
                font-size: 42px;
            }
            
            .products-grid {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
                gap: 30px;
            }
        }
        
        @media (max-width: 768px) {
            .store-header {
                padding: 15px 20px;
                flex-direction: column;
                gap: 20px;
            }
            .logo h1 {
                font-size: 25px;
                font-weight: bold;
            }
            .nav-links {
                width: 100%;
                justify-content: space-between;
            }
            
            .hero {
                height: 350px;
                margin-bottom: 40px;
            }
            
            .hero-content h2 {
                font-size: 36px;
            }
            
            .hero-content p {
                font-size: 16px;
            }
            
            .category-nav {
                padding: 0 20px;
                gap: 10px;
            }
            
            .category-nav a {
                padding: 10px 20px;
                width: 100%;
                text-align: center;
            }
            
            .products-grid {
                grid-template-columns: 1fr;
                padding: 0 20px;
                gap: 30px;
            }
            
            .cart-section {
                padding: 30px 20px;
                margin: 40px auto;
            }
            
            .cart-item {
                flex-direction: column;
                text-align: center;
                padding: 20px;
            }
            
            .item-image {
                margin-right: 0;
                margin-bottom: 20px;
            }
            
            .item-actions {
                margin-left: 0;
                margin-top: 20px;
            }
            
            .cart-summary {
                flex-direction: column;
                gap: 20px;
            }
            
            footer {
                padding: 40px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="store-container">
        <header class="store-header">
            <div class="logo">
                <i class="fas fa-dumbbell"></i>
                <h1>TMD M'bark</h1>
            </div>
            <nav class="nav-links">
                <a href="/Contact" class="dashboard-link">Contact Us</a>
                <a href="/store/Checkout" id="cart-toggle" class="cart-link">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="cart-count"><%= cartCount || 0 %></span>
                </a>
            </nav>
        </header>

        <main>
            <% if (messages && messages.error) { %>
                <div class="alert alert-error">
                    <i class="fas fa-exclamation-circle"></i> <%= messages.error %>
                </div>
            <% } %>
            <% if (messages && messages.success) { %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i> <%= messages.success %>
                </div>
            <% } %>

            <section class="hero">
                <div class="hero-content">
                    <h2>Elevate Your Training</h2>
                    <p>Premium equipment and supplements designed for peak performance</p>
                </div>
            </section>

            <section class="category-nav">
                <a href="/" class="<%= typeof category === 'undefined' ? 'active' : '' %>">All Products</a>
                <a href="/buy-service" class="<%= products.category === 'supplements' ? 'active' : '' %>">GYM MemberShip </a>
                <a href="/" class="<%= products.category === 'equipment' ? 'active' : '' %>">TMD</a>
                <a href="/About" class="<%= products.category === 'apparel' ? 'active' : '' %>">About Us</a>
                <a href="/Blog" class="<%= products.category === 'accessories' ? 'active' : '' %>">Media</a>
            </section>

            <section class="products-grid">
                <% products.forEach(product => { %>
                    <div class="product-card">
                        <div class="product-image">
                            <img src="<%= product.image.url %>" alt="<%= product.name %>">
                        </div>
                        <div class="product-info">
                            <h3><%= product.name %></h3>
                            <p><%= product.description %></p>
                            <div class="product-meta">
                                <span class="price"><%= product.price.toFixed(2) %> MAD</span>
                                <% if (product.stock > 0) { %>
                                    <span class="in-stock">In Stock</span>
                                <% } else { %>
                                    <span class="out-of-stock">Out of Stock</span>
                                <% } %>
                            </div>
                            <form action="/store/add-to-cart/<%= product._id %>" method="POST">
                                <button type="submit" class="btn-add-to-cart">
                                    <i class="fas fa-cart-plus"></i> Add to Cart
                                </button>
                            </form>
                        </div>
                    </div>
                <% }); %>
            </section>

            <!-- Cart Section -->
            <section class="cart-section" id="cart-section">
                <h2><i class="fas fa-shopping-cart"></i> Your Cart</h2>
                
                <% if (cart && cart.length === 0) { %>
                    <div class="empty-cart">
                        <p>Your cart is empty. Discover our premium products to begin.</p>
                        <a href="#" class="btn-continue" id="continue-shopping">Continue Shopping</a>
                    </div>
                <% } else { %>
                    <div class="cart-items">
                        <% cart.forEach(item => { %>
                            <div class="cart-item">
                                <div class="item-image">
                                    <img src="<%= item.image %>" alt="<%= item.name %>">
                                </div>
                                <div class="item-details">
                                    <h3><%= item.name %></h3>
                                    <p><%= item.price.toFixed(2) %> MAD</p>
                                    <div class="item-quantity">
                                        <span>Quantity: <%= item.quantity %></span> 
                                    </div>
                                </div>
                                <div class="item-actions">
                                    <form action="/store/remove-from-cart/<%= item.productId %>" method="POST">
                                        <button type="submit" class="btn-remove">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        <% }); %>
                        <a href="/store/Checkout" class="dashboard-link"> <i class="fas fa-shopping-cart"></i> BUY NOW</a>
                    </div>

 
                <% } %>
            </section>
        </main>

        <footer>
            <p>&copy; 2023 TMD M'bark | Premium Fitness Equipment & Supplements</p>
        </footer>
    </div>

    <script>
        // Toggle cart visibility
        document.getElementById('cart-toggle').addEventListener('click', function(e) {
            e.preventDefault();
            const cartSection = document.getElementById('cart-section');
            cartSection.style.display = cartSection.style.display === 'none' ? 'block' : 'none';
            
            // Smooth scroll to cart
            if (cartSection.style.display === 'block') {
                cartSection.scrollIntoView({ behavior: 'smooth' });
            }
        });

        // Continue shopping button
        document.getElementById('continue-shopping')?.addEventListener('click', function(e) {
            e.preventDefault();
            document.getElementById('cart-section').style.display = 'none';
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });

        // Show cart if there are items and messages
        window.addEventListener('DOMContentLoaded', function() {
            if ((cart && cart.length > 0) || (messages && (messages.error || messages.success))) { 
                document.getElementById('cart-section').style.display = 'block';
            } 
        });
    </script>
</body>
</html>
